# Neandercode Commit

Generate a terse commit message for the current staged changes.

Requirements:
- Conventional Commits format.
- Subject in imperative mood.
- Subject max 50 chars when possible (hard cap 72).
- Body only when non-obvious `why` is needed.
- Focus on intent/reason, not file-by-file narration.
