name: Sync SKILL.md and rules

on:
  push:
    branches: [main]
    paths:
      - skills/neandercode/SKILL.md
      - rules/neandercode-activate.md
      - neandercode-compress/SKILL.md
      - neandercode-compress/scripts/**

concurrency:
  group: sync-skill
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - name: Pull latest before making changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git pull --rebase origin main

      - name: Sync SKILL.md copies
        run: |
          cp skills/neandercode/SKILL.md neandercode/SKILL.md
          cp skills/neandercode/SKILL.md plugins/neandercode/skills/neandercode/SKILL.md
          cp skills/neandercode/SKILL.md .cursor/skills/neandercode/SKILL.md
          mkdir -p .windsurf/skills/neandercode
          cp skills/neandercode/SKILL.md .windsurf/skills/neandercode/SKILL.md

      - name: Sync compress skill
        run: |
          # Sync to skills/compress (main location for Gemini CLI)
          rm -rf skills/compress/scripts
          cp neandercode-compress/SKILL.md skills/compress/SKILL.md
          sed -i 's/^name: neandercode-compress$/name: compress/' skills/compress/SKILL.md
          sed -i "s|The compression scripts live in \`neandercode-compress/scripts/\` (adjacent to this SKILL.md). If the path is not immediately available, search for \`neandercode-compress/scripts/__main__.py\`.|This SKILL.md lives alongside \`scripts/\` in the same directory. Find that directory.|" skills/compress/SKILL.md
          sed -i 's|cd neandercode-compress && python3|cd <directory_containing_this_SKILL.md> \&\& python3|' skills/compress/SKILL.md
          cp -r neandercode-compress/scripts skills/compress/scripts
          rm -rf skills/compress/scripts/__pycache__

      - name: Sync compress skill to plugin
        run: |
          # Copy SKILL.md and patch name + process instructions for plugin context
          cp neandercode-compress/SKILL.md plugins/neandercode/skills/compress/SKILL.md
          sed -i 's/^name: neandercode-compress$/name: compress/' plugins/neandercode/skills/compress/SKILL.md
          sed -i "s|The compression scripts live in \`neandercode-compress/scripts/\` (adjacent to this SKILL.md). If the path is not immediately available, search for \`neandercode-compress/scripts/__main__.py\`.|This SKILL.md lives alongside \`scripts/\` in the same directory. Find that directory.|" plugins/neandercode/skills/compress/SKILL.md
          sed -i 's|cd neandercode-compress && python3|cd <directory_containing_this_SKILL.md> \&\& python3|' plugins/neandercode/skills/compress/SKILL.md
          # Copy scripts verbatim
          rm -rf plugins/neandercode/skills/compress/scripts
          cp -r neandercode-compress/scripts plugins/neandercode/skills/compress/scripts
          rm -rf plugins/neandercode/skills/compress/scripts/__pycache__

      - name: Rebuild neandercode.skill ZIP
        run: cd skills && zip -r ../neandercode.skill neandercode/

      - name: Sync auto-activation rules
        run: |
          BODY="rules/neandercode-activate.md"

          # Cline + Copilot — no frontmatter, direct copy
          mkdir -p .clinerules .github
          cp "$BODY" .clinerules/neandercode.md
          cp "$BODY" .github/copilot-instructions.md

          # Cursor — needs alwaysApply frontmatter
          mkdir -p .cursor/rules
          printf '%s\n' '---' 'description: "Neandercode mode — terse communication, ~75% fewer tokens, full technical accuracy"' 'alwaysApply: true' '---' '' > .cursor/rules/neandercode.mdc
          cat "$BODY" >> .cursor/rules/neandercode.mdc

          # Windsurf — needs always_on frontmatter
          mkdir -p .windsurf/rules
          printf '%s\n' '---' 'trigger: always_on' '---' '' > .windsurf/rules/neandercode.md
          cat "$BODY" >> .windsurf/rules/neandercode.md

      - name: Commit and push if changed
        run: |
          git add \
            neandercode/SKILL.md \
            skills/compress/ \
            plugins/neandercode/skills/compress/ \
            plugins/neandercode/skills/neandercode/SKILL.md \
            .cursor/skills/neandercode/SKILL.md \
            .windsurf/skills/neandercode/SKILL.md \
            neandercode.skill \
            .clinerules/neandercode.md \
            .github/copilot-instructions.md \
            .cursor/rules/neandercode.mdc \
            .windsurf/rules/neandercode.md
          git diff --staged --quiet && exit 0
          git commit -m "chore: sync SKILL.md copies and auto-activation rules [skip ci]"
          git push
