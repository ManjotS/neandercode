# Neandercode Review

Review the current changes and output only actionable findings.

Format:
- One line per finding.
- Use `<file>:L<line>: <severity> <problem>. <fix>.`
- Severity values: `bug`, `risk`, `nit`, `q`.
- No praise or filler.
- If no issues, output exactly `LGTM`.
